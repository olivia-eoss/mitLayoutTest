---
import { Image } from "astro:assets";
import guid from "../assets/hev-hero-guide.png";
import meeting from "../assets/hev-hero-meeting.png";
---

<section class="hero">
  <div class="hero__container">
    <div class="hero__content">
      <h1 class="hero__title">Mitgliederverwaltung mit Metasfresh</h1>
      <p class="hero__text">
        Schritt-für-Schritt-Anleitungen für die strukturierte Verwaltung Ihrer
        Mitglieder, Rechnungen und Zahlungen. Verstehen Sie Workflows,
        automatisieren Sie Prozesse und behalten Sie jederzeit den Überblick
        über Ihre ERP-Daten.
      </p>
      <a href="https://metasfresh.com" class="hero__button">
        zu metasfresh
        <svg
          width="20"
          height="20"
          viewBox="0 0 20 20"
          fill="none"
          xmlns="http://www.w3.org/2000/svg"
        >
          <path
            d="M7.5 15L12.5 10L7.5 5"
            stroke="currentColor"
            stroke-width="2"
            stroke-linecap="round"
            stroke-linejoin="round"></path>
        </svg>
      </a>
    </div>

    <div class="hero__images">
      <Image src={guid} alt="Guide Book" class="hero__image hero__image--1" />
      <Image
        src={meeting}
        alt="Team Meeting"
        class="hero__image hero__image--2"
      />
    </div>
  </div>
</section>

<style>
  .hero {
    padding: 5rem 2rem;
    background: var(--color-white);
    display: flex;
    align-items: center;
    margin-bottom: 80px;
    margin-top: 40px;
  }

  .hero__container {
    max-width: 1600px;
    margin: 0 auto;
    display: grid;
    grid-template-columns: 1fr 1fr;
    gap: 5rem;
    align-items: center;
  }

  .hero__content {
    max-width: 600px;
    opacity: 0;
    animation: slideInLeft 0.8s ease-out forwards;
  }

  .hero__title {
    color: var(--color-green);
    font-size: var(--fs-h1);
    font-weight: 700;
    line-height: 1.1;
    margin-bottom: 1.5rem;
  }

  .hero__text {
    color: var(--color-black);
    font-size: var(--fs-p);
    line-height: 1.6;
    margin-bottom: 2rem;
    opacity: 0.9;
  }

  .hero__button {
    display: inline-flex;
    align-items: center;
    gap: 0.5rem;
    background: var(--color-green);
    color: var(--color-white);
    padding: 0.875rem 2rem;
    border-radius: 6px;
    font-weight: 600;
    font-size: var(--fs-h4);
    text-decoration: none;
    transition: all 0.3s ease;
  }

  .hero__button:hover {
    background: #267047;
    transform: translateY(-2px);
    box-shadow: 0 8px 20px rgba(46, 133, 85, 0.3);
  }

  .hero__button svg {
    transition: transform 0.3s ease;
  }

  .hero__button:hover svg {
    transform: translateX(4px);
  }

  .hero__images {
    display: grid;
    grid-template-columns: 1fr 1fr;
    gap: 1.5rem;
    align-items: center;
  }

  .hero__image {
    width: 100%;
    height: auto;
    border-radius: 16px;
    box-shadow: 0 8px 30px rgba(0, 0, 0, 0.12);
    opacity: 0;
  }

  .hero__image--1 {
    animation: slideInRight 0.8s ease-out forwards;
  }

  .hero__image--2 {
    animation: slideInRight 0.8s ease-out forwards;
    margin-top: 2rem;
  }

  /* Animationen */
  @keyframes slideInLeft {
    from {
      opacity: 0;
      transform: translateX(-50px);
    }
    to {
      opacity: 1;
      transform: translateX(0);
    }
  }

  @keyframes slideInRight {
    from {
      opacity: 0;
      transform: translateX(50px) scale(0.95);
    }
    to {
      opacity: 1;
      transform: translateX(0) scale(1);
    }
  }

  /* Mobile */
  @media (max-width: 900px) {
    .hero {
      padding: 3rem 1.5rem;
      min-height: auto;
    }

    .hero__container {
      grid-template-columns: 1fr;
      gap: 3rem;
    }

    .hero__title {
      font-size: var(--fs-h1-m);
    }

    .hero__text {
      font-size: var(--fs-p-m);
    }

    .hero__content {
      animation: slideInUp 0.8s ease-out forwards;
    }

    .hero__images {
      gap: 1rem;
    }

    .hero__image--2 {
      margin-top: 1rem;
    }
  }

  @keyframes slideInUp {
    from {
      opacity: 0;
      transform: translateY(30px);
    }
    to {
      opacity: 1;
      transform: translateY(0);
    }
  }

  @media (max-width: 600px) {
    .hero__images {
      grid-template-columns: 1fr;
    }

    .hero__image--2 {
      margin-top: 0;
    }
  }
</style>
